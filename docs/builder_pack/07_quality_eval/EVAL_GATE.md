# Eval Gate

Beta eval suite minimum: 20 smoke tasks, 10 must-pass tasks, 5 schema validation tasks, 5 no-guess tasks.

No patch may enter stable unless all must-pass evals pass and score does not regress beyond accepted tolerance.

Required command: `heimdal eval run`.
