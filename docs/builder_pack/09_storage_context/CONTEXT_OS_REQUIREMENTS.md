# Context OS Requirements

Context OS builds Context Packets, enforces token budgets, selects truth snippets, working state, experience examples and skills, hashes packet contents, and logs references in Repro Pack.

If required context is unavailable: retrieve it, ask for input, or fail safely.
