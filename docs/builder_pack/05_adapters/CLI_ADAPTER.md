# CLI Adapter

Required commands: `heimdal run demo`, `heimdal run --input examples/tasks/simple_task.json`, and `heimdal doctor`.

The CLI adapter is the simplest host adapter and must work before OpenClaw integration.
