# Mirror Mode Requirements

Mirror Mode compares selected Heimdal outputs with a cloud teacher. Disabled by default. Must have daily token budget, privacy check, external-call logging, and produces patch proposals only.
