# Privacy and Mirror Policy

Privacy modes: local_only and cloud_allowed. Default is local_only. Mirror Mode must never send secrets, should redact sensitive content when configured, log external calls, and store distilled patch proposals.
