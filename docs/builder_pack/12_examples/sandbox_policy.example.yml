tool_sandbox:
  default_mode: read_only
  allow_shell: false
  allowed_paths: [./storage, ./artifacts, ./workspace]
  blocked_paths: [~/.ssh, ~/.openclaw/secrets, ~/.config, /etc]
  max_runtime_seconds: 60
  max_output_bytes: 200000
  network:
    default: off
    allowlist: []
